import 'package:flutter/material.dart';

class TabViewScreenOrders extends StatelessWidget {
  const TabViewScreenOrders({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Orders",
        style: TextStyle(fontSize: 30),
      ),
    );
  }
}
