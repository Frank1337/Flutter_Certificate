import 'package:flutter/material.dart';

class TabViewScreenUser extends StatelessWidget {
  const TabViewScreenUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "User",
        style: TextStyle(fontSize: 30),
      ),
    );
  }
}
